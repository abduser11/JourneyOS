/**
 * JourneyOS — Root Layout
 *
 * This file intentionally has no content.
 * All layout logic has been moved to route-group-specific layouts:
 *   - (marketing)/layout.tsx  — Public marketing pages
 *   - (auth)/layout.tsx       — Authentication pages
 *   - (protected)/layout.tsx  — Authenticated application shell
 *
 * Next.js requires at least one file in app/. This file satisfies that.
 */
export default function RootLayout() {
  return null;
}
