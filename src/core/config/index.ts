/**
 * JourneyOS — Core Configuration Module
 *
 * Centralized application configuration.
 * Re-exports from src/config/index.ts for consistency with
 * the core/ architectural pattern.
 */

export { config } from "@/config";
